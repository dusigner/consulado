/* global fixture: true, expect: true, beforeAll: true */
'use strict';

/*
 * Unit tests for src/Scripts/controllers/home.js
 */

describe('Home Controller', () => {

	it('should sum 1 plus 1 equal 2', () => {

		// Rly?!
		expect(1 + 1).toEqual(2);
	});
});
