/* global FB:true */

'use strict';

require('modules/helpers');
require('vendors/jquery.validate');
// require('bootstrap/tooltip');
require('vendors/jquery.maskedinput');

//load Nitro Lib
require('vendors/nitro');

Nitro.setup(['consul-landing-aniversario'], function () {
	var self = this;

	this.scrollTo = function(element) {
		var section = $(element).attr('href'),
			top = $(section).offset().top;

		$('html, body').animate({ scrollTop: top }, 600);
	};

	$('.header .container>div a:not(.share-facebook), #acoes-desktop .wrapper>div a:not(.share-facebook)').click(function(e){
		e.preventDefault();

		self.scrollTo($(this));
	});

	//compartilhar facebook
	$('.header .share-facebook').click(function(e) {
			e.preventDefault();

			// if(FB) {
			// 	FB.login(function(response) {
			// 			if (response.authResponse) {
			// 					FB.api('/me', function() {
			// 							FB.ui({
			// 									method: 'send',
			// 									link: 'http://loja.consul.com.br/landing/aniversario'
			// 							});
			// 					});
			// 			}
			// 	});
			// }
	});

	// //Iniciando SDK Facebook
	// window.fbAsyncInit = function () {
	// 		FB.init({
	// 				appId: store.isQA ? '183555032145633' : '178708989296904',
	// 				xfbml: true,
	// 				version: 'v2.8'
	// 		});
	// 		//FB.AppEvents.logPageView();
	// };

	// (function (d, s, id) {
	// 		var js, fjs = d.getElementsByTagName(s)[0];
	// 		if (d.getElementById(id)) { return; }
	// 		js = d.createElement(s); js.id = id;
	// 		js.src = '//connect.facebook.net/en_US/sdk.js';
	// 		fjs.parentNode.insertBefore(js, fjs);
	// }(document, 'script', 'facebook-jssdk'));
});